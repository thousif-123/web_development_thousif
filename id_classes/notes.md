## In css id have more preference than class.

### id is unique identifier to a element.

### two elements don't have same id but can have same classes.

#### A element can have multiple classes, so we have to write using spaces between every individual class name. 
